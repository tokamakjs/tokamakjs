export enum ModuleMetadataKeys {
  IMPORTS = 'imports',
  PROVIDERS = 'providers',
  ROUTING = 'routing',
  EXPORTS = 'exports',
}

export enum Scope {
  SINGLETON = 'SINGLETON',
  TRANSIENT = 'TRANSIENT',
}
